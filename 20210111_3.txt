#include <stdio.h>
int vzemiBitna(int number,int index);
int main(int argc, char *argv[]){
    int a,b;
    a=4;
    b=2;
    int rez=vzemiBitna(a,b);
printf("%d",rez);
return 0;
}
int vzemiBitna(int number,int index){
    int b=0; 
    int mask = 1 << index; 
    return (number & ~mask) | ((b << index) & mask); 
} 